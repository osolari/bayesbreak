# `bayesbreak` package interfaces

The modules in this directory expose approved names, typed records, and failure behavior. They do not implement the statistical algorithms. Every callable requiring scientific behavior raises `NotImplementedError` with the governing task ID.
