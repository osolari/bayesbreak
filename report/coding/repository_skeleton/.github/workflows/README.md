# Continuous integration

No workflow is supplied because CODE-BB-013 is incomplete. A future workflow must run the declared unit, property, numerical, statistical, regression, and reproducibility checks without representing timeouts as passes.
