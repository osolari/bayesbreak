# Data cards

Create one validated card per application. Each card must state source, acquisition date when known, coordinate definition, observation family, sequence count, preprocessing, weights or exposures, hashes, external annotation source if any, and limitations. No fitted MAP boundary may be labeled as an external event.
